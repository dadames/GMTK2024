class_name Modifier
extends Resource

@export var icon: Texture2D
@export var flyingText: String

# Put these in your modifiers
# func applies_to(node: Node) -> bool:
# func apply(node: Node) -> void:
# func unapply(node: Node) -> void:
